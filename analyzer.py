import anthropic

SYSTEM_PROMPT = """Ты — эксперт по Instagram-маркетингу с 10-летним опытом. Ты помогаешь авторам и брендам развивать аккаунты, создавать вовлекающий контент и выстраивать стратегию присутствия в социальных сетях.

Правила ответа:
- Всегда отвечай на русском языке
- Используй Markdown для форматирования
- Будь конкретным: давай реальные примеры тем, хуков, хэштегов
- Не давай общих советов — только практические действия
- В контент-плане создавай полноценную таблицу на указанное количество дней"""


def build_profile_prompt(data: dict, plan_days: int) -> str:
    return f"""Проанализируй Instagram-профиль и составь полный контент-план.

## Данные о профиле

- **Ниша / тематика:** {data['niche']}
- **Количество подписчиков:** {data['followers']:,}
- **Количество постов:** {data['posts_count']}
- **Целевая аудитория:** {data['target_audience'] or 'не указана'}
- **Цель аккаунта:** {data['goal']}
- **Тон общения:** {data['tone']}
- **Текущий контент (что уже выходит):** {data['current_content'] or 'не указано'}

## Что нужно сделать

Предоставь развёрнутый анализ и план, строго следуя этой структуре:

## Анализ профиля

**Сильные стороны** (минимум 3 пункта на основе данных)

**Слабые стороны / точки роста** (минимум 3 пункта)

**Целевая аудитория** — уточни портрет: возраст, интересы, боли, мечты

**Рекомендуемый Tone of Voice** — как говорить с аудиторией

---

## Контент-план на {plan_days} дней

Создай таблицу:

| День | Формат | Тема поста | Хук (первая фраза) | CTA |
|------|--------|-----------|-------------------|-----|
(заполни все {plan_days} строк)

Форматы чередуй: Рилс, Карусель, Фото, Сторис-серия, Экспертный пост

---

## Советы по росту

**Хэштеги** — дай 15–20 конкретных хэштегов, разбей на группы (нишевые, средние, широкие)

**Лучшее время публикаций** — укажи дни и часы

**Форматы с наибольшим охватом** — что сейчас работает в этой нише

**Быстрые wins** — 3 действия, которые дадут результат уже на этой неделе

---

## Идеи для постов и рилсов

Дай 5 конкретных идей с описанием:

**Идея 1 — [Название]**
- Формат: ...
- Тема: ...
- Хук: ...
- Структура: ...
- CTA: ...

(повтори для всех 5 идей)"""


def build_niche_prompt(data: dict, plan_days: int) -> str:
    return f"""Составь контент-стратегию для Instagram-аккаунта в нише.

## Вводные данные

- **Ниша:** {data['niche']}
- **Цель аккаунта:** {data['goal']}

## Что нужно сделать

Предоставь полный анализ ниши и контент-план, строго следуя этой структуре:

## Анализ ниши

**Возможности** — почему эта ниша перспективна в Instagram

**Целевая аудитория** — портрет: возраст, интересы, боли, мечты

**Рекомендуемый Tone of Voice** — как говорить с аудиторией

**Конкуренты и отличие** — как выделиться

---

## Контент-план на {plan_days} дней

Создай таблицу:

| День | Формат | Тема поста | Хук (первая фраза) | CTA |
|------|--------|-----------|-------------------|-----|
(заполни все {plan_days} строк)

Форматы чередуй: Рилс, Карусель, Фото, Сторис-серия, Экспертный пост

---

## Советы по росту

**Хэштеги** — дай 15–20 конкретных хэштегов, разбей на группы (нишевые, средние, широкие)

**Лучшее время публикаций** — укажи дни и часы

**Форматы с наибольшим охватом** — что сейчас работает в этой нише

**Быстрые wins** — 3 действия для старта с нуля

---

## Идеи для постов и рилсов

Дай 5 конкретных идей с описанием:

**Идея 1 — [Название]**
- Формат: ...
- Тема: ...
- Хук: ...
- Структура: ...
- CTA: ...

(повтори для всех 5 идей)"""


def stream_analysis(api_key: str, prompt: str):
    client = anthropic.Anthropic(api_key=api_key)

    with client.messages.stream(
        model="claude-sonnet-4-6",
        max_tokens=8096,
        system=[
            {
                "type": "text",
                "text": SYSTEM_PROMPT,
                "cache_control": {"type": "ephemeral"},
            }
        ],
        messages=[{"role": "user", "content": prompt}],
    ) as stream:
        for text in stream.text_stream:
            yield text
